# Dev environment overrides
# Merges on top of values.yaml

image:
  tag: "v0.2.1"
  pullPolicy: Always

replicaCount: 1

resources:
  limits:
    cpu: 250m
    memory: 128Mi
  requests:
    cpu: 10m
    memory: 64Mi

leaderElection:
  enabled: false

metrics:
  serviceMonitor:
    enabled: false
