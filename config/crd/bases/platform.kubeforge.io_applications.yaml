---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: applications.platform.kubeforge.io
spec:
  group: platform.kubeforge.io
  names:
    kind: Application
    listKind: ApplicationList
    plural: applications
    shortNames:
    - app
    singular: application
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Application is the Schema for the applications API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of Application
            properties:
              argoRollout:
                description: |-
                  argoRollout configures an Argo Rollout instead of a standard Deployment.
                  Requires Argo Rollouts to be installed in the cluster.
                properties:
                  replicas:
                    default: 1
                    description: replicas is the desired number of pod replicas.
                    format: int32
                    minimum: 1
                    type: integer
                  strategy:
                    description: strategy defines the rollout strategy.
                    properties:
                      blueGreen:
                        description: blueGreen defines a blue-green rollout strategy.
                        properties:
                          activeService:
                            description: activeService is the Service used to route
                              traffic to the active (blue) pods.
                            type: string
                          autoPromotionEnabled:
                            default: true
                            description: |-
                              autoPromotionEnabled automatically promotes the new version when ready.
                              Defaults to true.
                            type: boolean
                          postPromotionAnalysis:
                            description: postPromotionAnalysis defines analysis to
                              run after promotion.
                            properties:
                              args:
                                description: args are the arguments to pass to the
                                  AnalysisTemplate.
                                items:
                                  description: ArgoAnalysisArgument defines an argument
                                    for AnalysisTemplate.
                                  properties:
                                    name:
                                      description: name is the argument name.
                                      type: string
                                    value:
                                      description: value is the argument value.
                                      type: string
                                    valueFrom:
                                      description: valueFrom references a Pod value.
                                      properties:
                                        fieldRef:
                                          description: fieldRef references a field
                                            in the pod spec.
                                          properties:
                                            fieldPath:
                                              description: fieldPath is the path to
                                                the field (e.g., "metadata.labels['version']").
                                              type: string
                                          required:
                                          - fieldPath
                                          type: object
                                      required:
                                      - fieldRef
                                      type: object
                                  required:
                                  - name
                                  type: object
                                type: array
                              templates:
                                description: templates references AnalysisTemplate
                                  resources.
                                items:
                                  description: ArgoAnalysisTemplateRef references
                                    an AnalysisTemplate by name.
                                  properties:
                                    templateName:
                                      description: templateName is the name of the
                                        AnalysisTemplate.
                                      type: string
                                  required:
                                  - templateName
                                  type: object
                                type: array
                            required:
                            - templates
                            type: object
                          prePromotionAnalysis:
                            description: prePromotionAnalysis defines analysis to
                              run before promotion.
                            properties:
                              args:
                                description: args are the arguments to pass to the
                                  AnalysisTemplate.
                                items:
                                  description: ArgoAnalysisArgument defines an argument
                                    for AnalysisTemplate.
                                  properties:
                                    name:
                                      description: name is the argument name.
                                      type: string
                                    value:
                                      description: value is the argument value.
                                      type: string
                                    valueFrom:
                                      description: valueFrom references a Pod value.
                                      properties:
                                        fieldRef:
                                          description: fieldRef references a field
                                            in the pod spec.
                                          properties:
                                            fieldPath:
                                              description: fieldPath is the path to
                                                the field (e.g., "metadata.labels['version']").
                                              type: string
                                          required:
                                          - fieldPath
                                          type: object
                                      required:
                                      - fieldRef
                                      type: object
                                  required:
                                  - name
                                  type: object
                                type: array
                              templates:
                                description: templates references AnalysisTemplate
                                  resources.
                                items:
                                  description: ArgoAnalysisTemplateRef references
                                    an AnalysisTemplate by name.
                                  properties:
                                    templateName:
                                      description: templateName is the name of the
                                        AnalysisTemplate.
                                      type: string
                                  required:
                                  - templateName
                                  type: object
                                type: array
                            required:
                            - templates
                            type: object
                          previewService:
                            description: previewService is the Service used to route
                              traffic to the preview (green) pods.
                            type: string
                        required:
                        - activeService
                        type: object
                      canary:
                        description: canary defines a canary rollout strategy.
                        properties:
                          canaryService:
                            description: canaryService is the name of the Service
                              used to route traffic to canary pods.
                            type: string
                          stableService:
                            description: stableService is the name of the Service
                              used to route traffic to stable pods.
                            type: string
                          steps:
                            description: steps defines the canary rollout steps.
                            items:
                              description: ArgoCanaryStep defines a single canary
                                rollout step.
                              properties:
                                pause:
                                  description: pause defines a pause step.
                                  properties:
                                    duration:
                                      description: |-
                                        duration is the duration to pause (e.g., "5m", "1h").
                                        If empty, waits indefinitely until manually resumed.
                                      type: string
                                  type: object
                                setHeaderRoute:
                                  description: setHeaderRoute sets a header-based
                                    routing.
                                  properties:
                                    match:
                                      description: match defines the header match
                                        criteria.
                                      items:
                                        description: ArgoHeaderMatch defines a header
                                          match rule.
                                        properties:
                                          headerName:
                                            description: headerName is the header
                                              name to match.
                                            type: string
                                          headerValue:
                                            description: headerValue is the header
                                              value to match.
                                            properties:
                                              exact:
                                                description: exact is the exact header
                                                  value.
                                                type: string
                                              prefix:
                                                description: prefix is the prefix
                                                  to match.
                                                type: string
                                              regex:
                                                description: regex is the regex pattern
                                                  to match.
                                                type: string
                                            type: object
                                        required:
                                        - headerName
                                        - headerValue
                                        type: object
                                      type: array
                                    name:
                                      description: name is the name of the routing
                                        rule.
                                      type: string
                                  required:
                                  - match
                                  - name
                                  type: object
                                setMirror:
                                  description: setMirrorStep enables traffic mirroring.
                                  properties:
                                    percentage:
                                      description: percentage is the percentage of
                                        traffic to mirror.
                                      format: int32
                                      maximum: 100
                                      minimum: 0
                                      type: integer
                                  required:
                                  - percentage
                                  type: object
                                setWeight:
                                  description: setWeight sets the traffic weight percentage.
                                  format: int32
                                  type: integer
                              type: object
                            type: array
                          trafficRouting:
                            description: trafficRouting defines the traffic routing
                              configuration.
                            properties:
                              alb:
                                description: alb configures AWS ALB traffic routing.
                                properties:
                                  ingress:
                                    additionalProperties:
                                      type: string
                                    description: ingress is the Ingress annotation
                                      settings.
                                    type: object
                                  rootService:
                                    description: rootService is the root service for
                                      the ALB.
                                    type: string
                                required:
                                - rootService
                                type: object
                              istio:
                                description: istio configures Istio traffic routing.
                                properties:
                                  virtualServices:
                                    description: virtualServices references Istio
                                      VirtualServices.
                                    items:
                                      description: ArgoIstioVirtualService references
                                        an Istio VirtualService.
                                      properties:
                                        name:
                                          description: name is the VirtualService
                                            name.
                                          type: string
                                        routes:
                                          description: routes are the routes within
                                            the VirtualService.
                                          items:
                                            type: string
                                          type: array
                                      required:
                                      - name
                                      - routes
                                      type: object
                                    type: array
                                type: object
                              nginx:
                                description: nginx configures NGINX traffic routing.
                                properties:
                                  additionalIngressAnnotations:
                                    additionalProperties:
                                      type: string
                                    description: additionalIngressAnnotations are
                                      additional annotations for the canary Ingress.
                                    type: object
                                  stableIngress:
                                    description: stableIngress is the name of the
                                      stable Ingress.
                                    type: string
                                required:
                                - stableIngress
                                type: object
                              smi:
                                description: smi configures SMI traffic routing.
                                properties:
                                  trafficSplitName:
                                    description: trafficSplitName is the name of the
                                      TrafficSplit resource.
                                    type: string
                                required:
                                - trafficSplitName
                                type: object
                            type: object
                        type: object
                    type: object
                type: object
              env:
                description: |-
                  env defines non-secret environment variables for the primary container.
                  Secret references are deferred — only name/value pairs are supported.
                items:
                  description: |-
                    EnvVar represents a non-secret environment variable.
                    This is intentionally not corev1.EnvVar — secret/field references are deferred.
                  properties:
                    name:
                      description: name of the environment variable.
                      type: string
                    value:
                      description: value of the environment variable.
                      type: string
                  required:
                  - name
                  type: object
                type: array
              horizontalPodAutoscaler:
                description: |-
                  horizontalPodAutoscaler configures HorizontalPodAutoscaler for the application.
                  Requires resource requests to be set on the container.
                properties:
                  behavior:
                    description: behavior configures the scaling behavior of the target.
                    properties:
                      scaleDown:
                        description: |-
                          scaleDown is scaling policy for scaling Down.
                          If not set, the default value is to allow to scale down to minReplicas pods, with a
                          300 second stabilization window (i.e., the highest recommendation for
                          the last 300sec is used).
                        properties:
                          policies:
                            description: |-
                              policies is a list of potential scaling polices which can be used during scaling.
                              If not set, use the default values:
                              - For scale up: allow doubling the number of pods, or an absolute change of 4 pods in a 15s window.
                              - For scale down: allow all pods to be removed in a 15s window.
                            items:
                              description: HPAScalingPolicy is a single policy which
                                must hold true for a specified past interval.
                              properties:
                                periodSeconds:
                                  description: |-
                                    periodSeconds specifies the window of time for which the policy should hold true.
                                    PeriodSeconds must be greater than zero and less than or equal to 1800 (30 min).
                                  format: int32
                                  type: integer
                                type:
                                  description: type is used to specify the scaling
                                    policy.
                                  type: string
                                value:
                                  description: |-
                                    value contains the amount of change which is permitted by the policy.
                                    It must be greater than zero
                                  format: int32
                                  type: integer
                              required:
                              - periodSeconds
                              - type
                              - value
                              type: object
                            type: array
                            x-kubernetes-list-type: atomic
                          selectPolicy:
                            description: |-
                              selectPolicy is used to specify which policy should be used.
                              If not set, the default value Max is used.
                            type: string
                          stabilizationWindowSeconds:
                            description: |-
                              stabilizationWindowSeconds is the number of seconds for which past recommendations should be
                              considered while scaling up or scaling down.
                              StabilizationWindowSeconds must be greater than or equal to zero and less than or equal to 3600 (one hour).
                              If not set, use the default values:
                              - For scale up: 0 (i.e. no stabilization is done).
                              - For scale down: 300 (i.e. the stabilization window is 300 seconds long).
                            format: int32
                            type: integer
                          tolerance:
                            anyOf:
                            - type: integer
                            - type: string
                            description: |-
                              tolerance is the tolerance on the ratio between the current and desired
                              metric value under which no updates are made to the desired number of
                              replicas (e.g. 0.01 for 1%). Must be greater than or equal to zero. If not
                              set, the default cluster-wide tolerance is applied (by default 10%).

                              For example, if autoscaling is configured with a memory consumption target of 100Mi,
                              and scale-down and scale-up tolerances of 5% and 1% respectively, scaling will be
                              triggered when the actual consumption falls below 95Mi or exceeds 101Mi.

                              This is an beta field and requires the HPAConfigurableTolerance feature
                              gate to be enabled.
                            pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                            x-kubernetes-int-or-string: true
                        type: object
                      scaleUp:
                        description: |-
                          scaleUp is scaling policy for scaling Up.
                          If not set, the default value is the higher of:
                            * increase no more than 4 pods per 60 seconds
                            * double the number of pods per 60 seconds
                          No stabilization is used.
                        properties:
                          policies:
                            description: |-
                              policies is a list of potential scaling polices which can be used during scaling.
                              If not set, use the default values:
                              - For scale up: allow doubling the number of pods, or an absolute change of 4 pods in a 15s window.
                              - For scale down: allow all pods to be removed in a 15s window.
                            items:
                              description: HPAScalingPolicy is a single policy which
                                must hold true for a specified past interval.
                              properties:
                                periodSeconds:
                                  description: |-
                                    periodSeconds specifies the window of time for which the policy should hold true.
                                    PeriodSeconds must be greater than zero and less than or equal to 1800 (30 min).
                                  format: int32
                                  type: integer
                                type:
                                  description: type is used to specify the scaling
                                    policy.
                                  type: string
                                value:
                                  description: |-
                                    value contains the amount of change which is permitted by the policy.
                                    It must be greater than zero
                                  format: int32
                                  type: integer
                              required:
                              - periodSeconds
                              - type
                              - value
                              type: object
                            type: array
                            x-kubernetes-list-type: atomic
                          selectPolicy:
                            description: |-
                              selectPolicy is used to specify which policy should be used.
                              If not set, the default value Max is used.
                            type: string
                          stabilizationWindowSeconds:
                            description: |-
                              stabilizationWindowSeconds is the number of seconds for which past recommendations should be
                              considered while scaling up or scaling down.
                              StabilizationWindowSeconds must be greater than or equal to zero and less than or equal to 3600 (one hour).
                              If not set, use the default values:
                              - For scale up: 0 (i.e. no stabilization is done).
                              - For scale down: 300 (i.e. the stabilization window is 300 seconds long).
                            format: int32
                            type: integer
                          tolerance:
                            anyOf:
                            - type: integer
                            - type: string
                            description: |-
                              tolerance is the tolerance on the ratio between the current and desired
                              metric value under which no updates are made to the desired number of
                              replicas (e.g. 0.01 for 1%). Must be greater than or equal to zero. If not
                              set, the default cluster-wide tolerance is applied (by default 10%).

                              For example, if autoscaling is configured with a memory consumption target of 100Mi,
                              and scale-down and scale-up tolerances of 5% and 1% respectively, scaling will be
                              triggered when the actual consumption falls below 95Mi or exceeds 101Mi.

                              This is an beta field and requires the HPAConfigurableTolerance feature
                              gate to be enabled.
                            pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                            x-kubernetes-int-or-string: true
                        type: object
                    type: object
                  maxReplicas:
                    description: maxReplicas is the maximum number of pod replicas.
                    format: int32
                    minimum: 1
                    type: integer
                  metrics:
                    description: |-
                      metrics contains custom metrics for scaling.
                      If provided, targetCPU/targetMemory are ignored.
                    items:
                      description: |-
                        MetricSpec specifies how to scale based on a single metric
                        (only `type` and one other matching field should be set at once).
                      properties:
                        containerResource:
                          description: |-
                            containerResource refers to a resource metric (such as those specified in
                            requests and limits) known to Kubernetes describing a single container in
                            each pod of the current scale target (e.g. CPU or memory). Such metrics are
                            built in to Kubernetes, and have special scaling options on top of those
                            available to normal per-pod metrics using the "pods" source.
                          properties:
                            container:
                              description: container is the name of the container
                                in the pods of the scaling target
                              type: string
                            name:
                              description: name is the name of the resource in question.
                              type: string
                            target:
                              description: target specifies the target value for the
                                given metric
                              properties:
                                averageUtilization:
                                  description: |-
                                    averageUtilization is the target value of the average of the
                                    resource metric across all relevant pods, represented as a percentage of
                                    the requested value of the resource for the pods.
                                    Currently only valid for Resource metric source type
                                  format: int32
                                  type: integer
                                averageValue:
                                  anyOf:
                                  - type: integer
                                  - type: string
                                  description: |-
                                    averageValue is the target value of the average of the
                                    metric across all relevant pods (as a quantity)
                                  pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                  x-kubernetes-int-or-string: true
                                type:
                                  description: type represents whether the metric
                                    type is Utilization, Value, or AverageValue
                                  type: string
                                value:
                                  anyOf:
                                  - type: integer
                                  - type: string
                                  description: value is the target value of the metric
                                    (as a quantity).
                                  pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                  x-kubernetes-int-or-string: true
                              required:
                              - type
                              type: object
                          required:
                          - container
                          - name
                          - target
                          type: object
                        external:
                          description: |-
                            external refers to a global metric that is not associated
                            with any Kubernetes object. It allows autoscaling based on information
                            coming from components running outside of cluster
                            (for example length of queue in cloud messaging service, or
                            QPS from loadbalancer running outside of cluster).
                          properties:
                            metric:
                              description: metric identifies the target metric by
                                name and selector
                              properties:
                                name:
                                  description: name is the name of the given metric
                                  type: string
                                selector:
                                  description: |-
                                    selector is the string-encoded form of a standard kubernetes label selector for the given metric
                                    When set, it is passed as an additional parameter to the metrics server for more specific metrics scoping.
                                    When unset, just the metricName will be used to gather metrics.
                                  properties:
                                    matchExpressions:
                                      description: matchExpressions is a list of label
                                        selector requirements. The requirements are
                                        ANDed.
                                      items:
                                        description: |-
                                          A label selector requirement is a selector that contains values, a key, and an operator that
                                          relates the key and values.
                                        properties:
                                          key:
                                            description: key is the label key that
                                              the selector applies to.
                                            type: string
                                          operator:
                                            description: |-
                                              operator represents a key's relationship to a set of values.
                                              Valid operators are In, NotIn, Exists and DoesNotExist.
                                            type: string
                                          values:
                                            description: |-
                                              values is an array of string values. If the operator is In or NotIn,
                                              the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                              the values array must be empty. This array is replaced during a strategic
                                              merge patch.
                                            items:
                                              type: string
                                            type: array
                                            x-kubernetes-list-type: atomic
                                        required:
                                        - key
                                        - operator
                                        type: object
                                      type: array
                                      x-kubernetes-list-type: atomic
                                    matchLabels:
                                      additionalProperties:
                                        type: string
                                      description: |-
                                        matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                                        map is equivalent to an element of matchExpressions, whose key field is "key", the
                                        operator is "In", and the values array contains only "value". The requirements are ANDed.
                                      type: object
                                  type: object
                                  x-kubernetes-map-type: atomic
                              required:
                              - name
                              type: object
                            target:
                              description: target specifies the target value for the
                                given metric
                              properties:
                                averageUtilization:
                                  description: |-
                                    averageUtilization is the target value of the average of the
                                    resource metric across all relevant pods, represented as a percentage of
                                    the requested value of the resource for the pods.
                                    Currently only valid for Resource metric source type
                                  format: int32
                                  type: integer
                                averageValue:
                                  anyOf:
                                  - type: integer
                                  - type: string
                                  description: |-
                                    averageValue is the target value of the average of the
                                    metric across all relevant pods (as a quantity)
                                  pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                  x-kubernetes-int-or-string: true
                                type:
                                  description: type represents whether the metric
                                    type is Utilization, Value, or AverageValue
                                  type: string
                                value:
                                  anyOf:
                                  - type: integer
                                  - type: string
                                  description: value is the target value of the metric
                                    (as a quantity).
                                  pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                  x-kubernetes-int-or-string: true
                              required:
                              - type
                              type: object
                          required:
                          - metric
                          - target
                          type: object
                        object:
                          description: |-
                            object refers to a metric describing a single kubernetes object
                            (for example, hits-per-second on an Ingress object).
                          properties:
                            describedObject:
                              description: describedObject specifies the descriptions
                                of a object,such as kind,name apiVersion
                              properties:
                                apiVersion:
                                  description: apiVersion is the API version of the
                                    referent
                                  type: string
                                kind:
                                  description: 'kind is the kind of the referent;
                                    More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
                                  type: string
                                name:
                                  description: 'name is the name of the referent;
                                    More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names'
                                  type: string
                              required:
                              - kind
                              - name
                              type: object
                            metric:
                              description: metric identifies the target metric by
                                name and selector
                              properties:
                                name:
                                  description: name is the name of the given metric
                                  type: string
                                selector:
                                  description: |-
                                    selector is the string-encoded form of a standard kubernetes label selector for the given metric
                                    When set, it is passed as an additional parameter to the metrics server for more specific metrics scoping.
                                    When unset, just the metricName will be used to gather metrics.
                                  properties:
                                    matchExpressions:
                                      description: matchExpressions is a list of label
                                        selector requirements. The requirements are
                                        ANDed.
                                      items:
                                        description: |-
                                          A label selector requirement is a selector that contains values, a key, and an operator that
                                          relates the key and values.
                                        properties:
                                          key:
                                            description: key is the label key that
                                              the selector applies to.
                                            type: string
                                          operator:
                                            description: |-
                                              operator represents a key's relationship to a set of values.
                                              Valid operators are In, NotIn, Exists and DoesNotExist.
                                            type: string
                                          values:
                                            description: |-
                                              values is an array of string values. If the operator is In or NotIn,
                                              the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                              the values array must be empty. This array is replaced during a strategic
                                              merge patch.
                                            items:
                                              type: string
                                            type: array
                                            x-kubernetes-list-type: atomic
                                        required:
                                        - key
                                        - operator
                                        type: object
                                      type: array
                                      x-kubernetes-list-type: atomic
                                    matchLabels:
                                      additionalProperties:
                                        type: string
                                      description: |-
                                        matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                                        map is equivalent to an element of matchExpressions, whose key field is "key", the
                                        operator is "In", and the values array contains only "value". The requirements are ANDed.
                                      type: object
                                  type: object
                                  x-kubernetes-map-type: atomic
                              required:
                              - name
                              type: object
                            target:
                              description: target specifies the target value for the
                                given metric
                              properties:
                                averageUtilization:
                                  description: |-
                                    averageUtilization is the target value of the average of the
                                    resource metric across all relevant pods, represented as a percentage of
                                    the requested value of the resource for the pods.
                                    Currently only valid for Resource metric source type
                                  format: int32
                                  type: integer
                                averageValue:
                                  anyOf:
                                  - type: integer
                                  - type: string
                                  description: |-
                                    averageValue is the target value of the average of the
                                    metric across all relevant pods (as a quantity)
                                  pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                  x-kubernetes-int-or-string: true
                                type:
                                  description: type represents whether the metric
                                    type is Utilization, Value, or AverageValue
                                  type: string
                                value:
                                  anyOf:
                                  - type: integer
                                  - type: string
                                  description: value is the target value of the metric
                                    (as a quantity).
                                  pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                  x-kubernetes-int-or-string: true
                              required:
                              - type
                              type: object
                          required:
                          - describedObject
                          - metric
                          - target
                          type: object
                        pods:
                          description: |-
                            pods refers to a metric describing each pod in the current scale target
                            (for example, transactions-processed-per-second).  The values will be
                            averaged together before being compared to the target value.
                          properties:
                            metric:
                              description: metric identifies the target metric by
                                name and selector
                              properties:
                                name:
                                  description: name is the name of the given metric
                                  type: string
                                selector:
                                  description: |-
                                    selector is the string-encoded form of a standard kubernetes label selector for the given metric
                                    When set, it is passed as an additional parameter to the metrics server for more specific metrics scoping.
                                    When unset, just the metricName will be used to gather metrics.
                                  properties:
                                    matchExpressions:
                                      description: matchExpressions is a list of label
                                        selector requirements. The requirements are
                                        ANDed.
                                      items:
                                        description: |-
                                          A label selector requirement is a selector that contains values, a key, and an operator that
                                          relates the key and values.
                                        properties:
                                          key:
                                            description: key is the label key that
                                              the selector applies to.
                                            type: string
                                          operator:
                                            description: |-
                                              operator represents a key's relationship to a set of values.
                                              Valid operators are In, NotIn, Exists and DoesNotExist.
                                            type: string
                                          values:
                                            description: |-
                                              values is an array of string values. If the operator is In or NotIn,
                                              the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                              the values array must be empty. This array is replaced during a strategic
                                              merge patch.
                                            items:
                                              type: string
                                            type: array
                                            x-kubernetes-list-type: atomic
                                        required:
                                        - key
                                        - operator
                                        type: object
                                      type: array
                                      x-kubernetes-list-type: atomic
                                    matchLabels:
                                      additionalProperties:
                                        type: string
                                      description: |-
                                        matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                                        map is equivalent to an element of matchExpressions, whose key field is "key", the
                                        operator is "In", and the values array contains only "value". The requirements are ANDed.
                                      type: object
                                  type: object
                                  x-kubernetes-map-type: atomic
                              required:
                              - name
                              type: object
                            target:
                              description: target specifies the target value for the
                                given metric
                              properties:
                                averageUtilization:
                                  description: |-
                                    averageUtilization is the target value of the average of the
                                    resource metric across all relevant pods, represented as a percentage of
                                    the requested value of the resource for the pods.
                                    Currently only valid for Resource metric source type
                                  format: int32
                                  type: integer
                                averageValue:
                                  anyOf:
                                  - type: integer
                                  - type: string
                                  description: |-
                                    averageValue is the target value of the average of the
                                    metric across all relevant pods (as a quantity)
                                  pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                  x-kubernetes-int-or-string: true
                                type:
                                  description: type represents whether the metric
                                    type is Utilization, Value, or AverageValue
                                  type: string
                                value:
                                  anyOf:
                                  - type: integer
                                  - type: string
                                  description: value is the target value of the metric
                                    (as a quantity).
                                  pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                  x-kubernetes-int-or-string: true
                              required:
                              - type
                              type: object
                          required:
                          - metric
                          - target
                          type: object
                        resource:
                          description: |-
                            resource refers to a resource metric (such as those specified in
                            requests and limits) known to Kubernetes describing each pod in the
                            current scale target (e.g. CPU or memory). Such metrics are built in to
                            Kubernetes, and have special scaling options on top of those available
                            to normal per-pod metrics using the "pods" source.
                          properties:
                            name:
                              description: name is the name of the resource in question.
                              type: string
                            target:
                              description: target specifies the target value for the
                                given metric
                              properties:
                                averageUtilization:
                                  description: |-
                                    averageUtilization is the target value of the average of the
                                    resource metric across all relevant pods, represented as a percentage of
                                    the requested value of the resource for the pods.
                                    Currently only valid for Resource metric source type
                                  format: int32
                                  type: integer
                                averageValue:
                                  anyOf:
                                  - type: integer
                                  - type: string
                                  description: |-
                                    averageValue is the target value of the average of the
                                    metric across all relevant pods (as a quantity)
                                  pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                  x-kubernetes-int-or-string: true
                                type:
                                  description: type represents whether the metric
                                    type is Utilization, Value, or AverageValue
                                  type: string
                                value:
                                  anyOf:
                                  - type: integer
                                  - type: string
                                  description: value is the target value of the metric
                                    (as a quantity).
                                  pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                  x-kubernetes-int-or-string: true
                              required:
                              - type
                              type: object
                          required:
                          - name
                          - target
                          type: object
                        type:
                          description: |-
                            type is the type of metric source.  It should be one of "ContainerResource", "External",
                            "Object", "Pods" or "Resource", each mapping to a matching field in the object.
                          type: string
                      required:
                      - type
                      type: object
                    type: array
                  minReplicas:
                    description: minReplicas is the minimum number of pod replicas.
                    format: int32
                    minimum: 1
                    type: integer
                  targetCPUUtilizationPercentage:
                    description: |-
                      targetCPUUtilizationPercentage is the target average CPU utilization percentage.
                      Defaults to 80 if not specified and no custom metrics are provided.
                    format: int32
                    type: integer
                  targetMemoryUtilizationPercentage:
                    description: targetMemoryUtilizationPercentage is the target average
                      memory utilization percentage.
                    format: int32
                    type: integer
                required:
                - maxReplicas
                - minReplicas
                type: object
              image:
                description: image is the container image reference for the primary
                  application container.
                type: string
              kedaScaledObject:
                description: |-
                  kedaScaledObject configures a KEDA ScaledObject for event-driven autoscaling.
                  Requires KEDA to be installed in the cluster.
                properties:
                  cooldownPeriod:
                    default: 300
                    description: |-
                      cooldownPeriod is the period to wait after the last trigger reported active before scaling the resource back to 0.
                      Defaults to 300 seconds.
                    format: int32
                    type: integer
                  maxReplicaCount:
                    description: maxReplicaCount is the maximum number of replicas
                      KEDA will scale to.
                    format: int32
                    minimum: 1
                    type: integer
                  minReplicaCount:
                    description: minReplicaCount is the minimum number of replicas
                      KEDA will scale to.
                    format: int32
                    minimum: 0
                    type: integer
                  pollingInterval:
                    default: 30
                    description: |-
                      pollingInterval is the interval to check each trigger source on.
                      Defaults to 30 seconds.
                    format: int32
                    type: integer
                  triggers:
                    description: triggers defines the scaling triggers.
                    items:
                      description: KEDATrigger defines a KEDA scaling trigger.
                      properties:
                        authenticationRef:
                          description: authenticationRef specifies a TriggerAuthentication
                            reference.
                          properties:
                            name:
                              description: name is the name of the TriggerAuthentication
                                resource.
                              type: string
                            namespace:
                              description: namespace is the namespace of the TriggerAuthentication.
                                If omitted, uses the ScaledObject's namespace.
                              type: string
                          required:
                          - name
                          type: object
                        metadata:
                          additionalProperties:
                            type: string
                          description: metadata contains the trigger metadata as key-value
                            pairs.
                          type: object
                        type:
                          description: type is the type of trigger (e.g., "prometheus",
                            "kafka", "cpu", "memory").
                          type: string
                      required:
                      - metadata
                      - type
                      type: object
                    type: array
                required:
                - maxReplicaCount
                - minReplicaCount
                - triggers
                type: object
              networkPolicy:
                description: networkPolicy configures NetworkPolicy for the application
                  pods.
                properties:
                  egress:
                    description: egress defines allowed egress traffic from the application
                      pods.
                    items:
                      description: NetworkPolicyEgressRule defines an egress rule
                        for NetworkPolicy.
                      properties:
                        ports:
                          description: ports specifies the ports to allow.
                          items:
                            description: NetworkPolicyPort defines a port for NetworkPolicy.
                            properties:
                              endPort:
                                description: endPort is the end of a port range.
                                format: int32
                                type: integer
                              port:
                                anyOf:
                                - type: integer
                                - type: string
                                description: port is the port number or name.
                                x-kubernetes-int-or-string: true
                              protocol:
                                description: protocol is the protocol (TCP, UDP, SCTP).
                                type: string
                            type: object
                          type: array
                        to:
                          description: to specifies the destinations the pods can
                            access.
                          items:
                            description: NetworkPolicyPeer defines a peer for NetworkPolicy
                              ingress/egress.
                            properties:
                              ipBlock:
                                description: ipBlock selects IP blocks (CIDR).
                                properties:
                                  cidr:
                                    description: cidr is the CIDR range.
                                    type: string
                                  except:
                                    description: except specifies CIDR ranges to exclude.
                                    items:
                                      type: string
                                    type: array
                                required:
                                - cidr
                                type: object
                              namespaceSelector:
                                description: namespaceSelector selects namespaces.
                                properties:
                                  matchExpressions:
                                    description: matchExpressions is a list of label
                                      selector requirements. The requirements are
                                      ANDed.
                                    items:
                                      description: |-
                                        A label selector requirement is a selector that contains values, a key, and an operator that
                                        relates the key and values.
                                      properties:
                                        key:
                                          description: key is the label key that the
                                            selector applies to.
                                          type: string
                                        operator:
                                          description: |-
                                            operator represents a key's relationship to a set of values.
                                            Valid operators are In, NotIn, Exists and DoesNotExist.
                                          type: string
                                        values:
                                          description: |-
                                            values is an array of string values. If the operator is In or NotIn,
                                            the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                            the values array must be empty. This array is replaced during a strategic
                                            merge patch.
                                          items:
                                            type: string
                                          type: array
                                          x-kubernetes-list-type: atomic
                                      required:
                                      - key
                                      - operator
                                      type: object
                                    type: array
                                    x-kubernetes-list-type: atomic
                                  matchLabels:
                                    additionalProperties:
                                      type: string
                                    description: |-
                                      matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                                      map is equivalent to an element of matchExpressions, whose key field is "key", the
                                      operator is "In", and the values array contains only "value". The requirements are ANDed.
                                    type: object
                                type: object
                                x-kubernetes-map-type: atomic
                              podSelector:
                                description: podSelector selects pods in the same
                                  namespace.
                                properties:
                                  matchExpressions:
                                    description: matchExpressions is a list of label
                                      selector requirements. The requirements are
                                      ANDed.
                                    items:
                                      description: |-
                                        A label selector requirement is a selector that contains values, a key, and an operator that
                                        relates the key and values.
                                      properties:
                                        key:
                                          description: key is the label key that the
                                            selector applies to.
                                          type: string
                                        operator:
                                          description: |-
                                            operator represents a key's relationship to a set of values.
                                            Valid operators are In, NotIn, Exists and DoesNotExist.
                                          type: string
                                        values:
                                          description: |-
                                            values is an array of string values. If the operator is In or NotIn,
                                            the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                            the values array must be empty. This array is replaced during a strategic
                                            merge patch.
                                          items:
                                            type: string
                                          type: array
                                          x-kubernetes-list-type: atomic
                                      required:
                                      - key
                                      - operator
                                      type: object
                                    type: array
                                    x-kubernetes-list-type: atomic
                                  matchLabels:
                                    additionalProperties:
                                      type: string
                                    description: |-
                                      matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                                      map is equivalent to an element of matchExpressions, whose key field is "key", the
                                      operator is "In", and the values array contains only "value". The requirements are ANDed.
                                    type: object
                                type: object
                                x-kubernetes-map-type: atomic
                            type: object
                          type: array
                      type: object
                    type: array
                  ingress:
                    description: ingress defines allowed ingress traffic to the application
                      pods.
                    items:
                      description: NetworkPolicyIngressRule defines an ingress rule
                        for NetworkPolicy.
                      properties:
                        from:
                          description: from specifies the sources allowed to access
                            the pods.
                          items:
                            description: NetworkPolicyPeer defines a peer for NetworkPolicy
                              ingress/egress.
                            properties:
                              ipBlock:
                                description: ipBlock selects IP blocks (CIDR).
                                properties:
                                  cidr:
                                    description: cidr is the CIDR range.
                                    type: string
                                  except:
                                    description: except specifies CIDR ranges to exclude.
                                    items:
                                      type: string
                                    type: array
                                required:
                                - cidr
                                type: object
                              namespaceSelector:
                                description: namespaceSelector selects namespaces.
                                properties:
                                  matchExpressions:
                                    description: matchExpressions is a list of label
                                      selector requirements. The requirements are
                                      ANDed.
                                    items:
                                      description: |-
                                        A label selector requirement is a selector that contains values, a key, and an operator that
                                        relates the key and values.
                                      properties:
                                        key:
                                          description: key is the label key that the
                                            selector applies to.
                                          type: string
                                        operator:
                                          description: |-
                                            operator represents a key's relationship to a set of values.
                                            Valid operators are In, NotIn, Exists and DoesNotExist.
                                          type: string
                                        values:
                                          description: |-
                                            values is an array of string values. If the operator is In or NotIn,
                                            the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                            the values array must be empty. This array is replaced during a strategic
                                            merge patch.
                                          items:
                                            type: string
                                          type: array
                                          x-kubernetes-list-type: atomic
                                      required:
                                      - key
                                      - operator
                                      type: object
                                    type: array
                                    x-kubernetes-list-type: atomic
                                  matchLabels:
                                    additionalProperties:
                                      type: string
                                    description: |-
                                      matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                                      map is equivalent to an element of matchExpressions, whose key field is "key", the
                                      operator is "In", and the values array contains only "value". The requirements are ANDed.
                                    type: object
                                type: object
                                x-kubernetes-map-type: atomic
                              podSelector:
                                description: podSelector selects pods in the same
                                  namespace.
                                properties:
                                  matchExpressions:
                                    description: matchExpressions is a list of label
                                      selector requirements. The requirements are
                                      ANDed.
                                    items:
                                      description: |-
                                        A label selector requirement is a selector that contains values, a key, and an operator that
                                        relates the key and values.
                                      properties:
                                        key:
                                          description: key is the label key that the
                                            selector applies to.
                                          type: string
                                        operator:
                                          description: |-
                                            operator represents a key's relationship to a set of values.
                                            Valid operators are In, NotIn, Exists and DoesNotExist.
                                          type: string
                                        values:
                                          description: |-
                                            values is an array of string values. If the operator is In or NotIn,
                                            the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                            the values array must be empty. This array is replaced during a strategic
                                            merge patch.
                                          items:
                                            type: string
                                          type: array
                                          x-kubernetes-list-type: atomic
                                      required:
                                      - key
                                      - operator
                                      type: object
                                    type: array
                                    x-kubernetes-list-type: atomic
                                  matchLabels:
                                    additionalProperties:
                                      type: string
                                    description: |-
                                      matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                                      map is equivalent to an element of matchExpressions, whose key field is "key", the
                                      operator is "In", and the values array contains only "value". The requirements are ANDed.
                                    type: object
                                type: object
                                x-kubernetes-map-type: atomic
                            type: object
                          type: array
                        ports:
                          description: ports specifies the ports to allow.
                          items:
                            description: NetworkPolicyPort defines a port for NetworkPolicy.
                            properties:
                              endPort:
                                description: endPort is the end of a port range.
                                format: int32
                                type: integer
                              port:
                                anyOf:
                                - type: integer
                                - type: string
                                description: port is the port number or name.
                                x-kubernetes-int-or-string: true
                              protocol:
                                description: protocol is the protocol (TCP, UDP, SCTP).
                                type: string
                            type: object
                          type: array
                      type: object
                    type: array
                  policyTypes:
                    description: |-
                      policyTypes specifies the types of policies to apply.
                      Valid values: "Ingress", "Egress". Defaults to ["Ingress"] if only ingress defined, ["Egress"] if only egress defined, or both if both defined.
                    items:
                      type: string
                    type: array
                type: object
              podDisruptionBudget:
                description: podDisruptionBudget configures a PodDisruptionBudget
                  for high availability.
                properties:
                  maxUnavailable:
                    anyOf:
                    - type: integer
                    - type: string
                    description: |-
                      maxUnavailable specifies the maximum number of pods that can be unavailable.
                      Can be an absolute number or percentage (e.g., "25%").
                    x-kubernetes-int-or-string: true
                  minAvailable:
                    anyOf:
                    - type: integer
                    - type: string
                    description: |-
                      minAvailable specifies the minimum number of pods that must be available.
                      Can be an absolute number or percentage (e.g., "50%").
                    x-kubernetes-int-or-string: true
                type: object
              probes:
                description: probes configures liveness and readiness probes for the
                  primary container.
                properties:
                  liveness:
                    description: liveness configures the liveness probe.
                    properties:
                      failureThreshold:
                        default: 3
                        description: |-
                          failureThreshold is the number of consecutive failures before the probe is considered failed.
                          Defaults to 3.
                        format: int32
                        type: integer
                      httpGet:
                        description: httpGet specifies the HTTP GET action to perform.
                        properties:
                          httpHeaders:
                            description: httpHeaders are custom headers to set in
                              the request.
                            items:
                              description: HTTPHeader represents an HTTP header.
                              properties:
                                name:
                                  description: name of the header.
                                  type: string
                                value:
                                  description: value of the header.
                                  type: string
                              required:
                              - name
                              - value
                              type: object
                            type: array
                          path:
                            description: path is the HTTP path to GET.
                            type: string
                          port:
                            description: port is the port number to connect to.
                            format: int32
                            maximum: 65535
                            minimum: 1
                            type: integer
                          scheme:
                            description: |-
                              scheme is the HTTP scheme to use (HTTP or HTTPS).
                              Defaults to HTTP.
                            type: string
                        required:
                        - path
                        - port
                        type: object
                      initialDelaySeconds:
                        default: 10
                        description: |-
                          initialDelaySeconds is the number of seconds after the container starts before the probe is initiated.
                          Defaults to 10.
                        format: int32
                        type: integer
                      periodSeconds:
                        default: 10
                        description: |-
                          periodSeconds is how often to perform the probe.
                          Defaults to 10.
                        format: int32
                        type: integer
                      successThreshold:
                        default: 1
                        description: |-
                          successThreshold is the number of consecutive successes before the probe is considered successful.
                          Defaults to 1.
                        format: int32
                        type: integer
                      timeoutSeconds:
                        default: 1
                        description: |-
                          timeoutSeconds is the number of seconds after which the probe times out.
                          Defaults to 1.
                        format: int32
                        type: integer
                    type: object
                  readiness:
                    description: readiness configures the readiness probe.
                    properties:
                      failureThreshold:
                        default: 3
                        description: |-
                          failureThreshold is the number of consecutive failures before the probe is considered failed.
                          Defaults to 3.
                        format: int32
                        type: integer
                      httpGet:
                        description: httpGet specifies the HTTP GET action to perform.
                        properties:
                          httpHeaders:
                            description: httpHeaders are custom headers to set in
                              the request.
                            items:
                              description: HTTPHeader represents an HTTP header.
                              properties:
                                name:
                                  description: name of the header.
                                  type: string
                                value:
                                  description: value of the header.
                                  type: string
                              required:
                              - name
                              - value
                              type: object
                            type: array
                          path:
                            description: path is the HTTP path to GET.
                            type: string
                          port:
                            description: port is the port number to connect to.
                            format: int32
                            maximum: 65535
                            minimum: 1
                            type: integer
                          scheme:
                            description: |-
                              scheme is the HTTP scheme to use (HTTP or HTTPS).
                              Defaults to HTTP.
                            type: string
                        required:
                        - path
                        - port
                        type: object
                      initialDelaySeconds:
                        default: 10
                        description: |-
                          initialDelaySeconds is the number of seconds after the container starts before the probe is initiated.
                          Defaults to 10.
                        format: int32
                        type: integer
                      periodSeconds:
                        default: 10
                        description: |-
                          periodSeconds is how often to perform the probe.
                          Defaults to 10.
                        format: int32
                        type: integer
                      successThreshold:
                        default: 1
                        description: |-
                          successThreshold is the number of consecutive successes before the probe is considered successful.
                          Defaults to 1.
                        format: int32
                        type: integer
                      timeoutSeconds:
                        default: 1
                        description: |-
                          timeoutSeconds is the number of seconds after which the probe times out.
                          Defaults to 1.
                        format: int32
                        type: integer
                    type: object
                type: object
              replicas:
                default: 1
                description: |-
                  replicas is the desired number of pod replicas.
                  Defaults to 1.
                format: int32
                minimum: 1
                type: integer
              resources:
                description: resources specifies resource requests and limits for
                  the primary container.
                properties:
                  claims:
                    description: |-
                      Claims lists the names of resources, defined in spec.resourceClaims,
                      that are used by this container.

                      This field depends on the
                      DynamicResourceAllocation feature gate.

                      This field is immutable. It can only be set for containers.
                    items:
                      description: ResourceClaim references one entry in PodSpec.ResourceClaims.
                      properties:
                        name:
                          description: |-
                            Name must match the name of one entry in pod.spec.resourceClaims of
                            the Pod where this field is used. It makes that resource available
                            inside a container.
                          type: string
                        request:
                          description: |-
                            Request is the name chosen for a request in the referenced claim.
                            If empty, everything from the claim is made available, otherwise
                            only the result of this request.
                          type: string
                      required:
                      - name
                      type: object
                    type: array
                    x-kubernetes-list-map-keys:
                    - name
                    x-kubernetes-list-type: map
                  limits:
                    additionalProperties:
                      anyOf:
                      - type: integer
                      - type: string
                      pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                      x-kubernetes-int-or-string: true
                    description: |-
                      Limits describes the maximum amount of compute resources allowed.
                      More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                    type: object
                  requests:
                    additionalProperties:
                      anyOf:
                      - type: integer
                      - type: string
                      pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                      x-kubernetes-int-or-string: true
                    description: |-
                      Requests describes the minimum amount of compute resources required.
                      If Requests is omitted for a container, it defaults to Limits if that is explicitly specified,
                      otherwise to an implementation-defined value. Requests cannot exceed Limits.
                      More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                    type: object
                type: object
              service:
                description: service configures a Service for the application.
                properties:
                  port:
                    description: port is the service port exposed by the Service.
                    format: int32
                    maximum: 65535
                    minimum: 1
                    type: integer
                  targetPort:
                    description: |-
                      targetPort is the container port to forward traffic to.
                      Defaults to port when not set.
                    format: int32
                    maximum: 65535
                    minimum: 1
                    type: integer
                required:
                - port
                type: object
              topologySpreadConstraints:
                description: topologySpreadConstraints configures topology spread
                  constraints for the pods.
                items:
                  description: TopologySpreadConstraint defines a topology spread
                    constraint.
                  properties:
                    labelSelector:
                      description: |-
                        labelSelector is used to find matching pods.
                        If not specified, the Application's managed labels are used.
                      properties:
                        matchExpressions:
                          description: matchExpressions is a list of label selector
                            requirements. The requirements are ANDed.
                          items:
                            description: |-
                              A label selector requirement is a selector that contains values, a key, and an operator that
                              relates the key and values.
                            properties:
                              key:
                                description: key is the label key that the selector
                                  applies to.
                                type: string
                              operator:
                                description: |-
                                  operator represents a key's relationship to a set of values.
                                  Valid operators are In, NotIn, Exists and DoesNotExist.
                                type: string
                              values:
                                description: |-
                                  values is an array of string values. If the operator is In or NotIn,
                                  the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                  the values array must be empty. This array is replaced during a strategic
                                  merge patch.
                                items:
                                  type: string
                                type: array
                                x-kubernetes-list-type: atomic
                            required:
                            - key
                            - operator
                            type: object
                          type: array
                          x-kubernetes-list-type: atomic
                        matchLabels:
                          additionalProperties:
                            type: string
                          description: |-
                            matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                            map is equivalent to an element of matchExpressions, whose key field is "key", the
                            operator is "In", and the values array contains only "value". The requirements are ANDed.
                          type: object
                      type: object
                      x-kubernetes-map-type: atomic
                    maxSkew:
                      description: maxSkew describes the degree to which pods may
                        be unevenly distributed.
                      format: int32
                      minimum: 1
                      type: integer
                    topologyKey:
                      description: topologyKey is the key of node labels.
                      type: string
                    whenUnsatisfiable:
                      default: DoNotSchedule
                      description: |-
                        whenUnsatisfiable indicates how to deal with a pod if it doesn't satisfy the spread constraint.
                        Options: DoNotSchedule (default), ScheduleAnyway.
                      type: string
                  required:
                  - maxSkew
                  - topologyKey
                  type: object
                type: array
            required:
            - image
            type: object
          status:
            description: status defines the observed state of Application
            properties:
              conditions:
                description: |-
                  conditions represent the observed state of the Application.
                  Condition types: Ready, Progressing, Degraded.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              desiredReplicas:
                description: desiredReplicas is the number of replicas the Application
                  was last reconciled to.
                format: int32
                type: integer
              observedGeneration:
                description: observedGeneration is the generation of the Application
                  most recently reconciled.
                format: int64
                type: integer
              phase:
                description: |-
                  phase summarizes the overall operational state.
                  One of: "", Ready, Progressing, Degraded.
                type: string
              readyReplicas:
                description: readyReplicas is the number of ready pod replicas observed.
                format: int32
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
