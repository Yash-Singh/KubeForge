# Phase 2 — GitOps and CI/CD

## Task 8 — CI checks

### What was done

Updated `.github/workflows/test.yml` to include all required CI checks per `specs/06-gitops-cicd.md`:

| Job | What it checks |
|---|---|
| `format` | `go fmt ./...` — fails if formatting differences exist |
| `lint` | `make lint-config` + `make lint` |
| `unit-tests` | `make test` (unit + envtest) |
| `manifest-verification` | `make manifests generate` — fails if generated files are dirty |
| `crd-validation` | `make manifests` — fails if CRD schemas are out of date |
| `helm` | `make helm-lint` + `make helm-template` |
| `container-build` | `make docker-build` |
| `security-scan` | `go mod verify`, `go vet ./...`, `govulncheck ./...` |

Created `.github/workflows/release.yml` for GHCR image publishing:
- Triggered on `push` with semver tags (`v*.*.*`)
- Logs in to `ghcr.io` using `secrets.GITHUB_TOKEN`
- Builds and pushes the Docker image with semver tags and `latest` convenience tag
- Uses `docker/metadata-action` for tag generation

### Required checks from spec (all covered)

- ✅ formatting
- ✅ static analysis/lint
- ✅ unit tests
- ✅ generated code/manifest verification
- ✅ CRD schema validation
- ✅ envtest
- ✅ Helm lint/template checks
- ✅ container build
- ✅ security/dependency scanning
- ✅ Kind E2E (in `test-e2e.yml`, not duplicated)

### Image strategy

Default public registry: **GHCR**. Images tagged with immutable release versions (`v0.1.0`, etc.). `latest` exists as a convenience tag.

### Verification

- `make helm-lint` — passes
- `make helm-template` — renders without errors
- All existing workflows (`lint.yml`, `test-e2e.yml`) unchanged