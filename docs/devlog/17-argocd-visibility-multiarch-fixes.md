# Devlog 17 — ArgoCD Resource Visibility & Multi-Arch Release Fixes

## What was done

### 1. CI Lint Fixes (7 issues)

Fixed all lint failures from `golangci-lint`:
- **gofmt (2)**: Alignment issues in struct literals in `application_builder.go`
- **modernize (4)**: `int32Ptr` wrapper replaced with `ptr.To()` (k8s.io/utils)
- **revive (1)**: `ptr` parameter name shadowed `k8s.io/utils/ptr` import — renamed to `val`

Files changed:
- `internal/controller/application_builder.go` — renamed `ptr` → `val` in `getOrDefault()`, gofmt fixes
- `internal/controller/application_builder_test.go` — replaced `int32Ptr()` with `ptr.To()`, removed helper
- `internal/controller/application_controller_test.go` — replaced `int32Ptr()` with `ptr.To()`

### 2. ArgoCD Deployment Degraded — Root Cause

The operator deployment was `ErrImagePull` because:
- v0.2.0 was built as **amd64-only**
- Kind cluster runs on **arm64** (Apple Silicon)
- `no match for platform in manifest: not found`

**Fix:** Updated ArgoCD Helm values to `v0.2.1` (multi-arch: amd64 + arm64).

### 3. ArgoCD Resource Visibility Investigation

**Problem:** User couldn't see Pods, Endpoints, HPA, NetworkPolicy, ScaledObject in ArgoCD UI.

**Root cause:** ArgoCD only tracks resources **defined in Helm chart templates**. Resources created at runtime by the operator or auto-generated by Kubernetes are invisible to ArgoCD.

| Resource | In Helm Chart? | ArgoCD tracks? | Reason |
|----------|---------------|----------------|--------|
| Deployment | Yes | Yes | Defined in template |
| Service | Yes | Yes | Defined in template |
| ServiceMonitor | Yes | Yes | Defined in template |
| CRD | Yes | Yes | Defined in template |
| SA, ClusterRole | Yes | Yes | Defined in template |
| Endpoints | No | No | Auto-generated by K8s for each Service |
| Pods | No | No | Sub-resource of Deployment |
| HPA | No | No | Created by operator at runtime |
| NetworkPolicy | No | No | Created by operator at runtime |
| ScaledObject | No | No | Created by operator at runtime (dynamic client) |

**Attempted fix:** Removed Endpoints/EndpointSlice from ArgoCD `resource.exclusions` config. This didn't work because ArgoCD only discovers resources that are in the Helm chart — removing an exclusion doesn't add new tracked resources.

**Conclusion:** This is a fundamental ArgoCD + operator architecture trade-off. The operator creates resources dynamically (via typed and dynamic clients), outside of Helm's declarative resource set. To see these in ArgoCD would require either:
1. Adding all child resources to the Helm chart (defeats operator purpose)
2. Creating a separate ArgoCD app for Application CRs
3. Using ArgoCD resource tracking annotations on operator-created resources

**Decision:** Accept current behavior. Operator-created resources are visible via `kubectl` and the operator's status subresource.

### 4. Kind Cluster — Single Node

The Kind cluster has only one node (`kind-control-plane`). There are no worker nodes, so the operator runs on the control-plane node. This is normal for Kind. In production (EKS/GKE), separate worker nodes would exist.

### 5. Devlog 16 — KEDA + Argo Rollouts

Created `docs/devlog/16-phase3-keda-argo-rollouts.md` documenting:
- KEDA ScaledObject integration (dynamic client approach)
- Argo Rollouts canary/blue-green strategies
- Architecture decision: dynamic client avoids dependency conflicts
- API examples for both features

## Current State

- Operator: `v0.2.1` running on `kind-control-plane`, 1/1 Ready
- ArgoCD: Synced + Healthy
- CI: Lint fixes committed and pushed
- v0.2.1: Released with multi-arch (amd64 + arm64)
- All Phase 3 features verified on live cluster

## Dev/Prod ArgoCD Setup — ApplicationSet + Helm

Used ArgoCD **ApplicationSet** with Helm value files to manage dev/prod environments:

```
deploy/argocd/
  applicationset.yaml     ← generates dev/prod apps from template
  project.yaml            ← shared project config

charts/platform-operator/
  values.yaml             ← defaults
  values-dev.yaml         ← dev overrides (tag: main, 1 replica, no ServiceMonitor)
  values-prod.yaml        ← prod overrides (tag: v0.2.1, 2 replicas, ServiceMonitor)
```

**ApplicationSet** uses a list generator with two elements (dev, prod). Each generates an ArgoCD Application that points to the same Helm chart but with different `values-*.yaml` files.

**Environment differences:**

| Setting | Dev | Prod |
|---------|-----|------|
| Image tag | `main` (to be pushed) | `v0.2.1` |
| Replicas | 1 | 2 |
| ServiceMonitor | disabled | enabled |
| Leader election | disabled | enabled |
| Resources | 250m/128Mi limit | 500m/128Mi limit |

**Lesson:** Values files must be committed and pushed to git before ArgoCD can use them — it reads from the remote repo, not local filesystem.

### ArgoCD Configmap Incident

When reverting the `resource.exclusions` change, the `argocd-cm` configmap was accidentally overwritten with only the `resource.exclusions` key, losing all `resource.customizations` settings. This caused ArgoCD application controller to fail with "configmap not found" errors. Fixed by restoring the full configmap with all original keys.

**Lesson:** Always use `kubectl get configmap -o yaml` before editing, and patch specific keys rather than replacing the entire configmap.
